version: "3"

services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: 123454321
      POSTGRES_DB: teste_lccv
    ports:
      - "5432:5432"
    volumes:
      - ./data:/var/lib/postgresql/data
